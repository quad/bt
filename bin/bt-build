#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'bt'

include BT::Cli

single_repo_cmd('build', 'Build all stages') do |r|
  while stage = r.head.pipeline.ready.first
    stage.build
    puts "#{stage.name}: #{stage.result}"
  end
end
